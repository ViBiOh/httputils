---

name: Dependabot
on:
  pull_request:
    types: [opened, reopened]
jobs:
  build:
    name: Merge when ready
    runs-on: ubuntu-latest
    steps:
      - name: Add comment
        run: |
          curl -q -sSL --max-time 10 \
            -X POST \
            -H "Authorization: token ${{ github.token }}" \
            -H "Content-Type: application/json" \
            https://api.github.com/${{ github.event.pull_request.repository.full_name }}/commits/{{ github.event.pull_request.head.sha }}/comments \
            -d '{"body": "@dependabot merge"}'
